#include<iostream>
using namespace std;

int main() {

    int x = 10;
    int y = 20;

    int result = (x > y) ? x : y ;
    cout << "the larger value is" << result <<endl ;
    return 0;



    // int age = 12;

    // (age > 18) ? cout << "can vote" : cout << "can not vote";
    //  return 0;

}